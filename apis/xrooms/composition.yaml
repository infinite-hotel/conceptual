apiVersion: apiextensions.crossplane.io/v1
kind: Composition
metadata:
  name: rooms.scalebeyond.rejekts.io
spec:
  compositeTypeRef:
    apiVersion: scalebeyond.rejekts.io/v1alpha1
    kind: XRoom
  mode: Pipeline
  pipeline:
  - functionRef:
      name: infinite-hotel-conceptualroom
    step: room
  - functionRef:
      name: crossplane-contrib-function-auto-ready
    step: crossplane-contrib-function-auto-ready
