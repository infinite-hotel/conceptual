apiVersion: apiextensions.crossplane.io/v1
kind: Composition
metadata:
  name: penthouses.scalebeyond.rejekts.io
spec:
  compositeTypeRef:
    apiVersion: scalebeyond.rejekts.io/v1alpha1
    kind: XPenthouse
  mode: Pipeline
  pipeline:
  - functionRef:
      name: infinite-hotel-conceptualpenthouse
    step: penthouse
  - functionRef:
      name: crossplane-contrib-function-auto-ready
    step: crossplane-contrib-function-auto-ready
